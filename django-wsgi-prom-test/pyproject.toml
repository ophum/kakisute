[project]
name = "django-wsgi-prom-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=5.2.7",
    "django-prometheus>=2.4.1",
]

[dependency-groups]
prod = [
    "gunicorn>=23.0.0",
]

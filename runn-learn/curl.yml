desc: Generated by `runn new`
runners:
  req: https://zenn.dev
steps:
  - desc: "変数で指定されたユーザーの記事の一覧を取得します"
    req:
      /api/articles?username={{ vars.username }}&count=2&order=latest:
        get:
          body: null
    test: |
      current.res.status == 200
      && len(current.res.body.articles) == 2
